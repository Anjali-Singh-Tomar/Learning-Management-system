package org.employdemy.library.lms.model;

public enum TransactionStatus {
    REQUESTED,
    BORROWED,
    RETURNED,
    RENEWED,
    RENEW_REQUESTED,
    RENEW_DECLINED,
    DECLINED,
    RETURN_REQUESTED,
    RETURN_DECLINED
}
